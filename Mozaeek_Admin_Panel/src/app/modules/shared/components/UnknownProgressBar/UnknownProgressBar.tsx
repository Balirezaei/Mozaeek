import React from 'react';

import classes from './UnknownProgressBar.module.scss';

const UnknownProgressBar: React.VFC = () => {
  return <div className={classes.bar} />;
};

export default UnknownProgressBar;
